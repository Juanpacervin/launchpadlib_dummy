from setuptools import setup, find_packages

setup(
    name='launchpadlib',
    version='1.0.0',
    packages=find_packages(),
    description='Dummy package to replace launchpadlib',
    install_requires=[],
)